name: "bolsa_familia"
version: "1.0.0"
config-version: 2

profile: "bolsa_familia"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  bolsa_familia:
    staging:
      +materialized: view
    marts:
      +materialized: table
